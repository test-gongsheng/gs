
## 2026-04-07 股票监控系统维护记录

### 1. 股票类型计算 Bug 修复
**问题：** `calculate_stock_type()` 调用不存在的函数 `get_stock_history`，导致所有股票都返回默认"普通"类型。

**修复：**
- 更正函数名为 `get_stock_kline`
- 添加 `calculated` 字段标记是否成功计算（避免把失败结果持久化）
- 成功计算后才保存到 `stocks.json`

### 2. 买卖点批量初始化
**规则：**
- 高波动股（vol_score > 60）：买点/卖点 = 当前价 ±10%
- 普通股（vol_score ≤ 60）：买点/卖点 = 当前价 ±7%

**结果：** 12只持仓股全部设置完成

### 3. 实时买卖点监控（前端轮询方案）
**实现：**
- 每30秒轮询 `/api/price-alerts` 接口
- 触发时：右下角弹窗 + 声音提醒 + 股票卡片高亮闪烁
- 页面右上角添加开关按钮，可手动开启/关闭
- 同一天同一股票只提醒一次（去重机制）

**文件变更：**
- `app.py`: 新增 `/api/price-alerts` 接口
- `app.js`: 新增监控逻辑和弹窗函数
- `style.css`: 新增高亮动画和弹窗样式

### 4. 定时任务配置（main session 推送）
| 时间 | 任务 | 状态 |
|------|------|------|
| 8:55 | 开盘前舆情分析 | 启用 |
| 8:55 | 刷新股票波动率类型 | 启用 |
| 15:35 | 收盘持仓分析报告 | 启用 |
| 每5分钟 | 买卖点监控 | 已暂停（网页端已实现） |

### 5. 发现：两套独立系统
- **stock-monitor-v2** (网页版)：刚完成买卖点配置
- **stock-assistant** (CLI版)：独立的定时任务系统，配置了飞书推送

两套系统数据不互通，需要确认是否整合。

---
