
## 2026-04-02 南向资金缓存调试

### 关键发现
**akshare 请求极慢**：获取腾讯 00700 南向资金数据耗时 **129.37秒**（超过2分钟）

### 缓存修复进展
1. **表结构修复**：`southbound_stock_cache` 表从7列改为6列（删除多余列）
2. **JSON序列化**：添加 `ensure_ascii=False` 和 numpy 类型转换
3. **移除错误调用**：删除对不存在的 `_clear_expired_cache()` 的调用
4. **添加详细调试日志**：`_get_cache` 和 `_set_cache` 添加执行步骤日志

### 缓存验证状态
- [x] `_set_cache` 成功保存：`[Cache] 已保存: 00700, 84条`
- [ ] 待验证：第二次点击是否命中缓存（`[Cache] 命中`）

### 根本问题
即使缓存工作正常，**首次加载仍需2分钟以上**，这是 akshare 数据源本身的性能问题，非代码问题。

### Git提交
- `beb48ff` debug(step4): 添加详细缓存调试日志
