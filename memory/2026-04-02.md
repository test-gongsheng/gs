
## 南向资金缓存问题调试 (23:52)

**问题现状：**
- 缓存表 `southbound_stock_cache` 结构正确，但查询时为空
- 用户反馈港股之间切换南向资金仍很慢，不是秒出
- 已添加自动预加载逻辑到 Flask 启动流程，但可能 Flask 未重启

**关键代码位置：**
- `/root/.openclaw/workspace/stock-monitor-v2/utils/southbound_capital.py`
  - `_init_cache_db()`: 创建缓存表
  - `_get_cache(stock_code)`: 查询缓存
  - `_set_cache(stock_code, data)`: 保存缓存
  - `get_southbound_stock_history()`: 获取数据并调用 `_set_cache`

**缓存表结构：**
```sql
CREATE TABLE IF NOT EXISTS southbound_stock_cache (
    stock_code TEXT PRIMARY KEY,
    stock_name TEXT,
    data_json TEXT,
    record_count INTEGER,
    created_at INTEGER,
    expires_at INTEGER
)
```

**待办：**
1. 确认 Flask 已重启，自动预加载逻辑生效
2. 检查预加载是否成功写入缓存表
3. 验证港股切换时缓存命中

**Git commits today:**
- `90856f3` debug(step4): 添加缓存保存调试日志
- `beb48ff` debug(step4): 添加详细缓存调试日志
- `5cd256a` Flask启动时自动后台预加载南向资金数据
- `2ed067d` 删除手动预加载脚本
