# 2026-03-23 记忆

## 股票监控系统数据源升级

### 上午工作（10:00-11:30）

**问题**：腾讯财经API数据不稳定，导致行情获取失败

**解决方案**：将主数据源从腾讯财经切换为 akshare
- akshare 基于东方财富数据，更稳定
- 保留腾讯API作为备用数据源
- 自动回退机制：akshare失败时自动切换到腾讯

**代码改动**：
- `utils/stock_quote.py` - 重写数据获取逻辑，集成akshare
- `app.py` - API格式兼容
- `static/js/app.js` - 休市判断优化

**验证数据**（实时）：
- 腾讯控股：505.5 HKD（-0.49%）✅
- 平安银行：10.61 CNY（-1.49%）✅

---

## 中轴价格Bug修复

### 问题发现
用户反馈中轴价格显示错误（显示为持仓成本387.34，而非真实中轴价格）

### 根因分析
1. 导入时把 `costPrice` 当成了 `pivotPrice`
2. `refreshAxisPrices()` 只更新前端和localStorage，没同步后端数据库

### 修复方案
1. `import.js` - 导入时不再预设中轴价格（设为0）
2. `app.js` - 刷新中轴价格后调用 `/api/stocks/{id}/axis` 同步保存到后端

### Git提交
- commit 97e56ed: 升级数据源为akshare
- commit e383440: 修复中轴价格使用持仓成本的问题

---

## 服务稳定性

akshare获取全市场数据较慢，偶尔会导致服务崩溃。已重启服务恢复正常。

**后续优化方向**：
- 改为按需获取（只获取持仓股票）
- 减少刷新频率
- 添加本地缓存机制
