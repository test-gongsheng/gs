# 股票监控系统架构重构计划

## 已修复的数据流问题 ✅
- 单一数据源 (appState.stocks)
- 单一渲染入口 (renderStockList())

## 待修复的系统性问题

### 1. 内存泄漏
- 多个 setInterval 没有清理

### 2. 状态同步
- selectedStock 和 stocks 可能不同步

### 3. 性能
- 每次全量重建 DOM

### 4. 错误处理
- 缺乏统一的降级策略

### 5. 模块化
- app.js 3000+ 行，职责混杂

---

## 执行计划

### 当天（立即执行）- P0
1. **停止南向资金补丁式修复**
2. **定位根本原因**：后端数据 vs 前端渲染
3. **最小改动修复**：让南向资金能正常工作

### 本周（P0 - 核心架构修复）
1. **状态管理层提取**
   - 统一竞态条件管理
   - 单一数据源订阅模式

2. **南向资金模块独立化**
   - southbound.js 自包含，外部零耦合
   - 内部处理所有竞态条件

3. **渲染函数参数化**
   - 不再依赖全局 appState.selectedStock
   - 显式传递参数

4. **内存泄漏修复**
   - 清理所有 setInterval
   - 添加组件生命周期管理

### 下周（P1 - 模块拆分）
1. **app.js 模块拆分**
   - state.js - 状态管理
   - render.js - 渲染逻辑
   - data.js - 数据获取
   - utils.js - 工具函数
   - init.js - 初始化

2. **数据流单向化**
   - 事件驱动替代直接函数调用
   - Store 订阅模式

3. **虚拟滚动**
   - 持仓列表性能优化

### 本月（P2 - 长期重构）
1. **考虑使用 React/Vue 等框架**
2. **添加单元测试**
3. **TypeScript 类型安全**
4. **统一的错误处理和降级策略**

---

## 南向资金专项修复

### 问题定位流程
1. 查看后端日志确认返回数据是否正确
2. 如果后端正确 → 检查前端渲染逻辑
3. 如果后端错误 → 修复代码匹配逻辑

### 修复原则
- 停止添加更多调试日志
- 用最小改动解决问题
- 修复后验证，不再反复

---

*计划制定日期：2026-04-02*
*基于前天架构分析整理*
